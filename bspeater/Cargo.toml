[package]
name = "bspeater"
version = "0.1.0"
edition = "2024"

[dependencies]
# sourcepak = { version = "0.3.0", features = ["revpk"] }
bevy = { version = "0.16.*", features = ["wayland"] }
bevy_fly_camera = "0.16.*"
rayon = "1.*"
