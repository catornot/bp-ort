[package]
name = "catalectic_cl"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
rrplug = { workspace = true }
shared = { workspace = true }
retour = { version = "0.3.1", features = ["static-detour"] }

[lib]
crate-type = ["cdylib"]
